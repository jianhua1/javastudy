server:
  port: 2000
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
       defaultZone: http://localhost:2000/eureka/
  instance:
    hostname: localhost
spring:
  application:
    name: myeureka
